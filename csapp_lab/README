***********************
CS:APP Data Lab
学生操作指南
***********************

你的目标：修改 bits.c 的副本，使其通过 btest 中的所有测试，且不违反任何编码规范。


*********
0. 文件：
*********

Makefile	- 生成 btest、fshow 和 ishow
README		- 本文件
bits.c		- 你需要修改并提交的文件
bits.h		- 头文件
btest.c		- btest 主程序
  btest.h	- 用于构建 btest
  decl.c	- 用于构建 btest
  tests.c       - 用于构建 btest
  tests-header.c- 用于构建 btest
dlc*		- 规则检查编译器（data lab compiler）可执行文件
driver.pl*	- 驱动程序，调用 btest 与 dlc 对 bits.c 自动评分
Driverhdrs.pm   - 可选的“Beat the Prof”竞赛所用头文件
fshow.c		- 查看浮点数表示的工具
ishow.c		- 查看整数表示的工具

***********************************************************
1. 修改 bits.c 并检查其是否符合 dlc 规范
***********************************************************

重要：动手之前请仔细阅读 bits.c 文件中的说明。其中给出的编码规则，
是你想拿满分就必须遵守的。

用 dlc 编译器（./dlc）自动检查你的 bits.c 是否符合编码规范：

       unix> ./dlc bits.c

如果代码没有问题，dlc 不输出任何内容。否则它会打印消息指出问题所在。
带 -e 开关运行 dlc：

    	unix> ./dlc -e bits.c

会让 dlc 打印每个函数使用的运算符数量统计。

一旦有了合法的解，就可以用 ./btest 程序测试其正确性。

*********************
2. 用 btest 测试
*********************

本目录下的 Makefile 会把你的 bits.c 与附加代码一起编译，生成名为
btest 的程序（也就是测试框架）。

编译并运行 btest 程序，输入：

    unix> make btest
    unix> ./btest [可选命令行参数]

每次修改 bits.c 后都需要重新编译 btest。跨平台迁移时，你应删掉旧版本的
btest 再生成新的，使用命令：

    unix> make clean
    unix> make btest

Btest 通过对每个函数运行数百万个测试用例来检查代码正确性。它会围绕众所周
知的边界情况做大量测试，例如整数题中的 Tmin 和零，浮点题中的零、inf，以及
非规格化数与规格化数的分界。当 btest 检测到某个函数出错时，它会打印失败的
测试、错误结果和期望结果，然后终止该函数的测试。

下面是 btest 的命令行选项：

  unix> ./btest -h
  Usage: ./btest [-hg] [-r <n>] [-f <name> [-1|-2|-3 <val>]*] [-T <time limit>]
    -1 <val>  Specify first function argument
    -2 <val>  Specify second function argument
    -3 <val>  Specify third function argument
    -f <name> Test only the named function
    -g        Format output for autograding with no error messages
    -h        Print this message
    -r <n>    Give uniform weight of n for all problems
    -T <lim>  Set timeout limit to lim

示例：

  测试所有函数的正确性并打印错误消息：
  unix> ./btest

  以紧凑形式测试所有函数，不打印错误消息：
  unix> ./btest -g

  测试函数 foo 的正确性：
  unix> ./btest -f foo

  使用指定参数测试函数 foo 的正确性：
  unix> ./btest -f foo -1 27 -2 0xf

Btest 不检查代码是否符合编码规范，那项工作请用 dlc。

*******************
3. 辅助程序
*******************

我们附带了 ishow 和 fshow 两个程序，分别帮助你解读整数和浮点数表示。
每个程序接受一个十进制或十六进制数作为参数。构建它们，输入：

    unix> make

用法示例：

    unix> ./ishow 0x27
    Hex = 0x00000027,	Signed = 39,	Unsigned = 39

    unix> ./ishow 27
    Hex = 0x0000001b,	Signed = 27,	Unsigned = 27

    unix> ./fshow 0x15213243
    Floating point value 3.255334057e-26
    Bit Representation 0x15213243, sign = 0, exponent = 0x2a, fraction = 0x213243
    Normalized.  +1.2593463659 X 2^(-85)

    linux> ./fshow 15213243
    Floating point value 2.131829405e-38
    Bit Representation 0x00e822bb, sign = 0, exponent = 0x01, fraction = 0x6822bb
    Normalized.  +1.8135598898 X 2^(-126)
